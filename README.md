# ABI

Shared ABI / interface definitions for GameModManager.

## Contents

- `include/gmm_abi_v1.h` — Version 1 plugin ABI (struct layout, function pointers, version negotiation).

## Usage

Core, Plugins, and third-party plugin authors include this header to build against the stable ABI.
